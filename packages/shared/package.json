{
  "name": "shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Exam AI platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^0.34.6",
    "@types/node": "^20.10.0"
  }
}
