{
  "name": "examen-ia",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter api start"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "vite": "^6.3.5"
  },
  "pnpm": {
    "workspaces": [
      "apps/*",
      "packages/*"
    ],
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
